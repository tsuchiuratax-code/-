name: Publish

on:
  workflow_dispatch:
    branches:
      - main

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
      packages: write
      issues: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .tool-versions
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build project
        run: bun run build

      - name: Get version
        id: version
        run: |
          VERSION=$(node -e "console.log(require('./package.json').version)")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Version: $VERSION"

      # Phase 1: GitHub Release
      - name: Check if GitHub Release exists
        id: check-release
        run: |
          VERSION=${{ steps.version.outputs.version }}
          if gh release view v$VERSION >/dev/null 2>&1; then
            echo "ℹ️  GitHub Release v$VERSION already exists - skipping"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "✅ GitHub Release v$VERSION does not exist - will create"
            echo "skip=false" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Git Tag
        if: steps.check-release.outputs.skip != 'true'
        run: |
          VERSION=${{ steps.version.outputs.version }}
          if ! git rev-parse v$VERSION >/dev/null 2>&1; then
            git tag v$VERSION
            git push origin v$VERSION
            echo "✅ Created and pushed tag v$VERSION"
          else
            echo "ℹ️  Tag v$VERSION already exists"
          fi

      - name: Package skill for release
        if: steps.check-release.outputs.skip != 'true'
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "freee-mcp version: ${VERSION}" > skills/freee-api-skill/VERSION.md
          cd skills/freee-api-skill
          zip -r ../../freee-api-skill.zip .
          rm VERSION.md
          echo "✅ Packaged skill with VERSION.md (v${VERSION})"

      - name: Collect contributors from community PRs
        if: steps.check-release.outputs.skip != 'true'
        id: contributors
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 前回のリリースタグを取得（現在のタグを除く）
          PREV_TAG=$(git tag --sort=-v:refname | grep '^v' | sed -n '2p')

          CONTRIBUTORS=""
          if [ -n "$PREV_TAG" ]; then
            PREV_DATE=$(git log -1 --format=%aI "$PREV_TAG")
            echo "Previous tag: $PREV_TAG ($PREV_DATE)"

            # 前回リリース以降に close された from-pr issue を検索
            USERS=$(gh issue list \
              --label "from-pr" \
              --state closed \
              --search "closed:>=$PREV_DATE" \
              --json body \
              --jq '.[].body' \
            | { grep -oP 'by @\K[^\s)+]+' || true; } \
            | sort -u)

            if [ -n "$USERS" ]; then
              CONTRIBUTORS=$(echo "$USERS" | while read -r user; do
                echo "- @${user}"
              done)
              echo "Found contributors: $USERS"
            fi
          else
            echo "No previous tag found, skipping contributor collection"
          fi

          {
            echo "list<<EOF"
            echo "$CONTRIBUTORS"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          # README の Contributors セクションに新しいユーザーを追加
          if [ -n "$USERS" ]; then
            while read -r user; do
              if ! grep -q "github.com/${user}" README.md; then
                sed -i "s|<!-- CONTRIBUTORS-END -->|<a href=\"https://github.com/${user}\"><img src=\"https://github.com/${user}.png\" width=\"40\" height=\"40\" alt=\"@${user}\"></a>\n<!-- CONTRIBUTORS-END -->|" README.md
                echo "Added $user to README contributors"
              fi
            done <<< "$USERS"

            if git diff --quiet README.md; then
              echo "No new contributors to add to README"
            else
              git config user.name "github-actions[bot]"
              git config user.email "github-actions[bot]@users.noreply.github.com"
              git add README.md
              git commit -m "docs: add new contributors to README"
              git push
              echo "Pushed README contributor updates"
            fi
          fi

      - name: Create GitHub Release
        if: steps.check-release.outputs.skip != 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }}
          body: |
            ## What's Changed

            See [CHANGELOG.md](./CHANGELOG.md) for details.
            ${{ steps.contributors.outputs.list != '' && format('\n## Contributors\n\nThank you to the following contributors for their suggestions!\n\n{0}\n', steps.contributors.outputs.list) || '' }}
            ## Package
            - npm: https://www.npmjs.com/package/freee-mcp/v/${{ steps.version.outputs.version }}

          files: freee-api-skill.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Phase 2: npm publish
      - name: Check if npm package exists
        id: check-npm
        run: |
          VERSION=${{ steps.version.outputs.version }}
          if npm view freee-mcp@$VERSION version 2>/dev/null; then
            echo "ℹ️  npm package freee-mcp@$VERSION already exists - skipping"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "✅ npm package freee-mcp@$VERSION does not exist - will publish"
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Publish to npm (Trusted Publishing)
        if: steps.check-npm.outputs.skip != 'true'
        run: bun run release
        env:
          NPM_CONFIG_PROVENANCE: true

      - name: Sync plugin.json version
        if: steps.check-npm.outputs.skip != 'true'
        run: |
          VERSION=${{ steps.version.outputs.version }}
          jq --arg v "$VERSION" '.version = $v' .claude-plugin/plugin.json > tmp.json && mv tmp.json .claude-plugin/plugin.json
          jq --arg v "$VERSION" '.plugins[0].version = $v' .claude-plugin/marketplace.json > tmp.json && mv tmp.json .claude-plugin/marketplace.json
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .claude-plugin/
          git commit -m "chore: sync plugin version to $VERSION" || echo "No changes to commit"
          git push
          echo "✅ Synced plugin.json version"
